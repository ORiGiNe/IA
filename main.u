var g1 = UGaop.new(tty);
var g2 = UGaop.new(tty);
var s = UCapteur.new(4,g1);
var c = UCapteur.new(22,g2);
var j = UCapteur.new(21,g2);
var a = UAsserv.new(78,g);
g.initialise;

var t = Tag.new;

waituntil(j.get == 1);

every (0.2s)
	if (s.get < 40) {
		t.freeze;
		sleep(2);
		t.unfreeze;
	};

a.avance (30);
if (c.get == 1)
	a.tourner (90)
else
	a.tourner (-90);
a.avance (190);
